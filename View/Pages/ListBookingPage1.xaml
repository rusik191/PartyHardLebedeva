<Page x:Class="ListBookingPage1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PartyHardLebedeva"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ListBookingPage1">
    <Grid>
        <ListView Margin="0,0,0,-0.4">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Комната" DisplayMemberBinding="{Binding Комната}" />
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Дата}" />
                    <GridViewColumn Header="Время" DisplayMemberBinding="{Binding Время}"/>
                    <GridViewColumn Header="ФИО" DisplayMemberBinding="{Binding ФИО}"/>
                    <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding телефон}"/>
                    

                </GridView>
            </ListView.View>

        </ListView>
    </Grid>
</Page>
